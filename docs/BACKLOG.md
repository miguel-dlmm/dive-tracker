# Ocean Flow — Backlog

> Prioridad actual del producto, no un registro histórico de lo ya hecho
> (eso vive en `CHANGELOG.md` y en el historial de git). Si una fila deja
> de ser cierta, se corrige o se borra — no se acumula.

Alcance del producto (instructor freelance, no B2B) ya decidido — ver
`docs/PRODUCT.md` y `docs/ADR/0001-alcance-instructor-freelance-no-b2b.md`.
Este backlog no vuelve a discutir esa decisión, solo construye dentro de
ella. Cualquier ítem de arquitectura o modelo de datos necesita su propio
ADR en `docs/ADR/` antes de implementarse, esté en "Ahora" o en "Después".

## Cómo leer esta tabla

- **Valor**: cuánto lo nota el instructor freelance, no cuánto cuesta construirlo.
- **Esfuerzo**: XS/S/M/L/XL.
- **Prioridad**: Ahora / Después / No hacer.

## Ahora

| Ítem | Problema que resuelve | Valor | Esfuerzo | Riesgo | Dependencia |
|---|---|---|---|---|---|
| Sembrar `payment_statuses` (Pending/Paid) en el alta + autoservicio del instructor | Cuenta nueva nace sin estados de pago y hoy no puede gestionarlos ella misma. La cuenta demo (`dev-bypass`) se corrigió a mano el 2026-08-28 tras confirmar que este era exactamente el problema — el hueco de fondo (ninguna cuenta nueva nace con estos 2 estados) sigue sin resolverse | Medio-Alto | S | Bajo | Ninguna |
| **Bug:** añadir tarifa inline bloquea el formulario de Mi trabajo | Ver nota de detalle debajo de la tabla — investigado a fondo el 2026-08-29 sin conseguir reproducirlo; sigue sin causa raíz confirmada | Alto (bloquea una acción frecuente; riesgo de pérdida de trabajo del usuario) | Necesita prueba en iPhone físico real — no reproducible con las herramientas disponibles en este entorno | Alto mientras no se reproduzca ni se confirme la causa, por tratarse de un flujo de uso diario | Prueba manual del usuario en su iPhone (ver nota) |

**Nota de bug — añadir tarifa inline bloquea el formulario** (registrado el 2026-08-28; investigado a fondo el 2026-08-29, sin conseguir reproducirlo ni confirmar causa raíz):

- **Comportamiento observado:** después de guardar una tarifa desde el bloque inline dentro del formulario de creación/edición de movimientos (`MovementSheet.jsx`, antes en `MiTrabajoTab.jsx`), la pantalla puede quedar completamente bloqueada durante un tiempo. A veces solo se recupera esperando bastante, o recargando la página, lo que hace perder el formulario en curso.
- **Contexto en el que ocurre:** al usar "Añadir tarifa" (`openInlineRate`/`saveRate` en `MovementSheet.jsx`) cuando la escuela+curso elegidos todavía no tienen una tarifa configurada.
- **Investigación realizada (2026-08-29):** se instrumentó un heartbeat de `requestAnimationFrame` (misma técnica que confirmó y resolvió el bug de animación de cobro esta misma sesión) para detectar de forma inequívoca un bloqueo real del hilo principal al guardar una tarifa inline, en dos escenarios: (1) guardado con la moneda por defecto sin tocar el selector, y (2) abriendo el selector de moneda, buscando y eligiendo una moneda distinta antes de guardar (la sospecha explícita del usuario). **En ningún caso se observó ningún hueco entre fotogramas superior a ~55ms** (muy por debajo del umbral de bloqueo real, ~500ms+) ni pérdida de capacidad de respuesta de la hoja tras guardar. Se revisó además el hook `useFloatingDropdown` (usado por el selector de moneda) en busca de listeners globales que pudieran quedar huérfanos al desmontar el bloque de tarifa inline justo después de guardar — su ciclo de vida es correcto (el listener de `pointerdown` se retira en el cleanup del efecto), no hay fuga ahí. Tampoco hay ninguna suscripción realtime ni bucle evidente en `useSupabaseTable.insertRow` (inserción simple + `setState` local).
- **Conclusión de la investigación:** no se ha encontrado ninguna causa en Chromium con datos realistas (11 tarifas, 5 registros de trabajo en la cuenta demo) ni interactuando con el selector de moneda. Esto no descarta el bug — lo acota: es coherente con ser específico del motor WebKit/Safari real o de la interacción con el teclado virtual de iOS (`visualViewport`), la misma clase de limitación que este proyecto ya tiene documentada y confirmada como ciega para las herramientas disponibles aquí (ver CLAUDE.md, "8. Verificación UX/UI" — WebKit cuelga en este entorno concreto, motivo por el que `mobile-check` usa Chromium). No se ha aplicado ningún workaround ni timeout — por instrucción explícita del usuario, y porque hacerlo sin causa confirmada arriesgaría enmascarar el problema real.
- **Siguiente paso real:** reproducir en un iPhone físico (Safari real), idealmente con las DevTools remotas de Safari conectadas para capturar un perfil de rendimiento en el momento exacto del bloqueo — es la única vía de diagnóstico que falta por agotar.

**Nota — "Ajuste de curso" y el campo de nombre de compañero** (evaluado el 2026-08-29, a petición del usuario, sin implementar nada nuevo): el campo `colleague_name` usa un `<input>` con `<datalist>` nativo del navegador (sugerencias de nombres ya usados en esa escuela, `colleagueSuggestions` en `MovementSheet.jsx`) — no el componente `SearchSelect`/panel flotante que usa el resto de la app. Revisado: no influye en el rendimiento (recomputa un array pequeño, sin listeners propios, mecanismo nativo del navegador) y es un flujo completamente distinto del de la tarifa inline (no comparten código). El "nunca funcionó bien" percibido es coherente con una limitación conocida y real de `<datalist>` en iOS Safari (soporte históricamente pobre/inconsistente de las sugerencias en móvil), no con un bug de esta app. No se propone ninguna funcionalidad nueva de explotación de datos por compañero (fuera de alcance, confirmado por el usuario) — si algún día se quiere una búsqueda con mejor comportamiento en iOS, sustituir el `<datalist>` por el `SearchSelect` ya existente sería la vía consistente con el resto de la app, pero no hay evidencia de que merezca prioridad hoy.

## Después

| Ítem | Problema que resuelve | Valor | Esfuerzo | Riesgo | Dependencia |
|---|---|---|---|---|---|
| Subir a Supabase Pro (backups diarios automáticos + PITR opcional) | Analizado el 2026-08-30 (ver `docs/ADR/0017-politica-de-backups-mvp.md`): el plan Free actual no incluye ninguna copia automática — hoy la única red de seguridad es el backup manual semanal (`npm run backup:db`), con RPO de hasta 7 días | Alto si el volumen/dependencia del negocio crece; bajo hoy (RPO de 7 días ya documentado y aceptado como suficiente para el uso actual) | Ninguno técnico — es solo cambiar de plan en el dashboard de Supabase | Ninguno — mejora pura de fiabilidad | Decisión de coste recurrente del usuario ($25/mes al cierre de esta sesión, verificar precio vigente) — no una necesidad técnica todavía |
| Crear rama `test` como entorno de integración | Permite validar varias features juntas antes de que las vea el grupo de prueba actual, sin usar `develop` para ello | Bajo hoy (sin evidencia de que haga falta) | S | Bajo | Ninguno de los disparadores de `docs/ADR/0006-estrategia-de-ramas-y-entornos.md` se ha cumplido todavía — no crear por adelantado |
| Crear rama `main` + producción pública real (dominio propio, posible Supabase separado) | Separa a los usuarios de prueba actuales de un futuro lanzamiento público más amplio | Alto el día que exista lanzamiento público, nulo hoy | M | Medio — implica decidir aislamiento de datos | Condicionado a que exista de verdad un lanzamiento público — ver `docs/ADR/0006-estrategia-de-ramas-y-entornos.md` |
| **"Mi trabajo" — Fase 2:** posible migración física del modelo (fusionar `worklog`/`comisiones`/`colleague_payments`) | Solo si, en uso real, el adaptador (`buildActivityEntries`) demuestra no ser suficiente — no por limpieza arquitectónica | Bajo hoy (sin evidencia de que haga falta) | L | Medio — migración de datos real, necesita ADR propio | Evidencia de uso real de "Mi trabajo" (Fase 1, ya en producción — ver `docs/ADR/0005-mi-trabajo-unificacion-economica.md`) |
| Eliminar `payment_type` del todo (frontend + esquema) | Causa raíz del incidente de altas nuevas; retira también el workaround temporal ya desplegado. **2026-08-30: confirmado con datos reales** (no solo teoría) — una cuenta cuyo catálogo `payment_types` no incluye "Per Person" (tiene sus propios nombres, p. ej. "Instructor") obtiene tarifas fijas de forma silenciosa al crear una tarifa en línea, sin ningún aviso — visto en un caso real ("Reef Divers – Adventure Dive", 150 fijo en vez de 150×personas). Ver addendum de `docs/ADR/0003` | Antes indirecto, ahora confirmado con un caso real — alto en fiabilidad | M | Bajo — plan incremental, ver `docs/ADR/0003-eliminar-payment-type.md` | Mejor después de sembrar `payment_statuses` — misma zona de catálogos |
| Pulido visual completo de Pagos (jerarquía, densidad, estilo) | Pagos ya tiene su estructura final (rediseñada como liquidación por escuela en `ADR-0005`) sin el pulido estético — inspirarse en la exploración ya hecha en `src/lab/` | Medio (percepción de calidad, no funcional) | M | Bajo | Ninguna — ya desplegada, lista para tomarse cuando toque. Decisión estética, no funcional — sin ADR propio salvo que surjan alternativas reales |
| Reutilizar componente entre Home y Resumen (calendario/agregación mensual) | La duplicación DENTRO de Resumen (dos calendarios casi iguales, uno global y uno por escuela) se eliminó en el rediseño de Resumen (2026-08-29, ver `docs/ADR/0009-rediseno-resumen.md`) — queda solo la duplicación ENTRE Home y Resumen, que ya usan el mismo `MonthCalendar` de `shared.jsx` pero con lógica de agregación mensual propia repetida en cada archivo | Bajo (invisible) | S | Bajo | Ninguna — hacerlo cuando se toque cualquiera de las dos pantallas por otro motivo |
| Animación de salida completa en `UserDetailSheet`/`CreateUserSheet`/`ActivationLinkPanel` (`ConfigTab.jsx`) | 2026-08-30 (bloque de gestos): las 3 usan ya el componente `Sheet` (motion + arrastrar para cerrar) igual que el resto de la app, pero sus padres las MONTAN/DESMONTAN por completo (`{cond && <Sheet.../>}`) en vez de mantenerlas siempre montadas con una prop `open` — la animación de ENTRADA y el gesto de arrastrar funcionan bien, pero al cerrar (X, backdrop o arrastre) el panel desaparece de golpe en vez de deslizarse hacia abajo, porque React lo desmonta antes de que Motion pueda terminar la transición de salida. Mismo patrón ya resuelto en `MovementSheet.jsx` (siempre montado, con `creating`/`form` como estado interno) — aplicar ahí exigiría que cada padre pase `open` en vez de dejar de renderizar el componente, y que cada hoja recuerde el último usuario/enlace mostrado mientras dura el cierre, para no quedarse en blanco a mitad de animación | Bajo (pantallas de administración, uso infrecuente; hoy sin animación de entrada NI de salida — con este bloque ya hay entrada+gesto, solo la salida queda a medias) | S-M por hoja — el propio patrón ya existe y probado en `MovementSheet.jsx`, es "copiarlo", no diseñarlo de cero | Bajo — cambio de presentación, sin tocar ninguna llamada a Supabase/API | Ninguna técnica — hacerlo si en uso real la desaparición brusca resulta molesta; documentado aquí para no reabrir la investigación desde cero |
| Decidir mitigación de `email_for_nickname` (RPC pública sin autenticar) | Superficie de enumeración de email — sin rate limit | Reducción de riesgo, no feature | S | Bajo hoy, crece si cambia el alcance de usuarios | Es una decisión de apetito de riesgo, no solo técnica |
| **"Actividades" → "Cursos", fase 2:** renombrar variables/props/componentes internos (`activities`, `activityColor`, `ACTIVITIES`...) | Fase 1 (solo texto visible) ya completada en toda la app (2026-08-29, Configuración/Tarifas/Home/Resumen — ver `docs/ADR/0008-rediseno-configuracion.md` y `docs/ADR/0009-rediseno-resumen.md`). Esta fase es solo consistencia interna de nombres, no cambia nada visible | Bajo (invisible para el usuario) | L — transversal a casi toda la app | Bajo pero mucha superficie de cambio | Ninguna técnica — hacerlo solo si aporta de verdad, no por limpieza |
| Registrar "fecha de baja" de un usuario (`profiles.deactivated_at`) | Pedido explícitamente al rediseñar Usuarios (2026-08-29, ver `docs/ADR/0014-usuarios-lista-y-detalle.md`) — hoy no existe ninguna columna que registre CUÁNDO se desactivó una cuenta; `auth.users.banned_until` guarda cuándo terminaría el baneo, no cuándo empezó, así que no sirve para derivarla | Medio (transparencia para el superadmin) | S — una columna nueva + escribirla/limpiarla en `server/users/setUserActive.js` | Bajo — columna nueva, sin tocar datos existentes | Ninguna — requiere solo aprobar el plan de migración (columna aditiva, no destructiva) antes de implementar |
| Healthcheck periódico de sesión de usuario | Si una cuenta se desactiva desde administración mientras el usuario ya está dentro de la app, la sesión puede seguir viva hasta que el `access_token` caduque (~1h) o se llame a un endpoint `auth.*` que sí detecte el baneo — ver el límite ya documentado en `docs/SESSION-2026-08-28-rediseno-global.md` (Bloque 10, "Límite conocido y aceptado"). Comprobaría periódicamente si la cuenta sigue activa, cerraría la sesión, llevaría al login y mostraría el mismo mensaje ya unificado de cuenta desactivada (`ACCOUNT_DEACTIVATED_MESSAGE`, `useSession.js`) | Bajo hoy (ventana acotada, un único usuario freelance por cuenta) | S-M — probablemente un `setInterval` que llame a `supabase.auth.getUser()` (no `getSession()`, que es local) y reutilice `resolveSessionState` | Bajo | Ninguna técnica — ya evaluado como sobreingeniería para el riesgo actual (Bloque 10); solo revisar si el perfil de riesgo cambia (más usuarios, cuentas compartidas) |
| Snapshot de tarifa en `worklog`/`comisiones` (importe deja de recalcularse en vivo) | Analizado el 2026-08-30, sin implementar (ver `docs/ADR/0016-tarifas-depreciacion-historica-analisis.md`): hoy editar o borrar una tarifa cambia retroactivamente el importe de todo movimiento pasado que la usara — un problema de integridad de datos financieros real (una venta pasada no debería cambiar de precio porque la lista de precios cambió después). La opción recomendada es snapshotear la tarifa resuelta en el propio movimiento al crearlo (como ya hace `colleague_payments` con `amount`), no un modelo completo de tarifas versionadas (desproporcionado para el tamaño actual del negocio) | Medio-Alto en fiabilidad de los datos financieros — el riesgo es silencioso (nadie lo nota hasta que revisa un mes cerrado) | M — migración de esquema (columna `rate` nueva + backfill) + cambio en `rateCalc.js`/`MovementSheet.jsx` | Medio — migración incremental, no destructiva, pero toca el cálculo central de la app | Mejor después de `ADR-0003` (eliminar `payment_type`) si esa también se ejecuta — snapshotear el cálculo ya simplificado es menos trabajo |
| **Configuración → Moneda favorita** (pantalla nueva) | 2026-08-30: la moneda dejó de elegirse por movimiento (Ajuste de curso, `MovementSheet.jsx`) — ahora se resuelve sola (favorita guardada en `localStorage`, ver ADR-0007, o la moneda por defecto de la app). Sin esta pantalla, no hay ningún sitio desde el que cambiar cuál es la favorita: antes existía un botón "Usar X como favorita" dentro del propio formulario, retirado junto con el campo de Moneda | Medio — sin esto, un instructor que empieza a cobrar en una moneda distinta se queda sin forma de cambiar la favorita desde la UI | S — reutiliza el mismo mecanismo ya existente (`getFavoriteCurrency`/clave `oceanpulse:favoriteCurrency:*`, `MovementSheet.jsx`); solo falta la pantalla que lo escriba | Bajo — preferencia personal en `localStorage`, no dato de negocio en Supabase | Ninguna técnica — priorizar si en uso real se confirma que hace falta cambiar de moneda favorita más de una vez |

## No hacer (por ahora)

| Ítem | Por qué | Qué lo reactivaría |
|---|---|---|
| Unificar `rates`/`commission_rates` en una tabla con tipo | Fuera del alcance de `ADR-0005` — "Mi trabajo" unifica la experiencia sobre `worklog`/`comisiones`/`colleague_payments` vía adaptador, sin tocar `rates`/`commission_rates`, que siguen siendo 2 tablas separadas detrás de `RatesTab` | Si la Fase 2 de `ADR-0005` (migración de modelo) llega a ejecutarse y en ese momento se justifica hacerlo junto |
| Migración física de `worklog`/`comisiones`/`colleague_payments` a una tabla única | Ya no es una hipótesis sin decidir — es la Fase 2 de `ADR-0005`, condicionada a que la Fase 1 (adaptador + UI, ya en producción) demuestre no ser suficiente | Ver condiciones en `docs/ADR/0005-mi-trabajo-unificacion-economica.md` |
| Separar navegación instructor / administración de plataforma | Ya resuelto por los gates de rol existentes; contradice el alcance decidido mientras el único admin seas tú | Ver condiciones en `docs/ADR/0001-alcance-instructor-freelance-no-b2b.md` |
| Vender/ceder cuentas a otros instructores como negocio | Pregunta de negocio, no de producto — no es un entregable técnico | Necesita un dueño de decisión de negocio, no ingeniería |
